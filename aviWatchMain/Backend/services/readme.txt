Services: Contain business logic, interact with databases or external APIs, and perform other tasks not directly related to handling HTTP requests.

You might have a user service that handles user-related operations (e.g., user creation, retrieval, update) that can be used by different controllers.
